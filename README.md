# game-1

Установить Git LFS

Если Linux (apt):
```bash
sudo apt install git-lfs
git lfs install
```

Если Windows, то через сайт, после:
```powershell
git lfs install
```
---
После pull/fetch если в проекте появился новый файл:
```
git lfs pull
```